interface LocationDelta {
    latitudDelta: number,
    longitudDelta: number,
}

interface MapCoordinates {
    latitude: number,
    longitude: number,
    latitudeDelta: number,
    longitudeDelta: number,
}

interface MarkLocation {
    latitude: number,
    longitude: number,
}
